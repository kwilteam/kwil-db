package client

/*
import (
	"github.com/ignite/cli/ignite/pkg/cosmosclient"
	"github.com/kwilteam/kwil-db/cmd/kwil-cosmos/x/kwil/types"
)

// Stake will take a map[string]interface{} and, if the "to" field equals the locally configured wallet, will send it to the cosmos chain
func (c *Client) Stake(ev map[string]interface{}) {
	msg := types.Msg
}
*/
