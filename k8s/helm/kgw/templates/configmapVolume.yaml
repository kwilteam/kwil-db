apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kgw.configMapVolume.name" . }}
  labels:
    {{- include "kgw.labels" . | nindent 4 }}
data:
  {{/* TODO: loop over .Values.configMapMnt */}}
  keys.json: |
{{ .Files.Get "files/keys.json" | indent 4 }}