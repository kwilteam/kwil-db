apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kwild.fullname" . }}-test-connection"
  labels:
    {{- include "kwild.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kwild.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
