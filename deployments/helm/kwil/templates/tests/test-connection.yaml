apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kwil.fullname" . }}-test-connection"
  labels:
    {{- include "kwil.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kwil.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
